const currentStatus = ['sold out', 'available'];
const role = ['Seller', 'Customer', 'Admin'];
const state = ['punjab', 'sindh', 'kpk', 'balochistan'];

module.exports = {
  currentStatus,
  role,
  state,
};
